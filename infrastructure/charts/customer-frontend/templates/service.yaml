apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-frontend.fullname" . }}
  labels:
    {{- include "customer-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customer-frontend.selectorLabels" . | nindent 4 }}